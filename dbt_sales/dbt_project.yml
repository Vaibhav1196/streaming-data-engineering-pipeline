name: dbt_sales
version: "1.0.0"
config-version: 2

profile: dbt_sales

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_sales:
    staging:
      +materialized: view
    marts:
      +materialized: table
